# 进度与规划

**本文档是唯一「交付状态」入口**：区分已实现、未实现与可选增强。  
**最近对齐代码**：2026-04-09（若实现有变，请同步改本节）。

---

## 1. 已完成（可在仓库中验收）

### 1.1 工程基线（原 V3 / M1–M4）

- **质量与 CI**：单测、`pnpm ci`（typecheck + test + smoke）、GitHub Actions。
- **工具链**：`ToolRegistry`、多 Provider、`ToolExecutionService`（守卫、超时、重试、trace）、内置 + Runtime Skill + **MCP（stdio）**；配置见 `ONECLAW_MCP_SERVERS` / `_FILE`。
- **可观测**：JSONL trace、CLI `pnpm cli trace …`。
- **安全**：路径 / exec 策略、`toolGuard`、审计脱敏；说明见 [安全配置与风险](./security-risks.md)。

### 1.2 任务与协作（V4 主线）

- **任务单据**：状态机、时间线、`meta.v4_plan` / `v4_last_review`、报告导出等。
- **按步骤工具白名单**：`PlanStep.allowedTools` 与 `ToolPolicyGuard`；Runner 编排见 `taskRunner.ts`；开关 `ONECLAW_M2_STEP_TOOL_ENFORCEMENT`（默认开启）。
- **Checkpoint 与恢复**：失败写检查点、`resume` / `resumeTaskFromCheckpoint`；时间线含 `step_start` / `step_done` / `step_failed` / `resume_from_checkpoint` 等。
- **高风险审批**：`ONECLAW_TASK_HIGH_RISK_APPROVAL`；`exec` / `apply_patch` 及 **`riskLevel === "high"`** 的工具可拦截；`approve` 后可将工具名记入 **本任务内** 后续免审集合（`meta` 中的 grants）。
- **任务上下文聊天**：`taskId` 关联会话与任务上下文注入；工具拒绝可写 `tool_denied`；工具结束可写结构化 timeline 步骤（与 trace 字段对齐，见 `chatProcessing` / `taskService`）。
- **Skills 条件启用**：`enableWhen` + `loadSkillsForContext`（见 [架构与扩展](./架构与扩展.md)）。

### 1.3 测试覆盖（与实现手册对应）

- 策略、Runner、恢复相关：**单元测试**见 `tests/stepToolPolicy.test.ts`、`tests/taskRunner.test.ts` 等。
- 文档 [`archive/specs/m2-production-runner-executor.md`](./archive/specs/m2-production-runner-executor.md) 中建议的 **固定 E2E 场景（fix_bug / code_review）** 若以独立 E2E 套件衡量，仍建议按需补全（见下方「未完成」）。

---

## 2. 未完成或仅部分满足（相对产品愿景 / 旧 PRD）

下列多为 **体验、规模化或自动化** 层面，核心链路已可用不代表以下已做满。

| 方向 | 说明 |
|------|------|
| **三角色运行时隔离** | 仍有结构化协议与 API；**无**三个独立 Agent 运行时或「Planner 绝不调高风险工具」的硬隔离进程级保证。 |
| **全自动编排服务** | 非「一键从规划 → 多轮执行 → 评审分支」的独立编排服务；当前以 API + Runner + 聊天侧执行为主。 |
| **审批产品化** | 无独立审批队列服务、多任务优先级、审批人角色绑定；策略维度仍以 env + 任务 meta 为主。 |
| **模板与向导** | 模板参数与仓库/分支的 **校验 UI / 向导** 未做。 |
| **Web 任务看板** | API 支持 `failedOnly` 等；**无**独立「仅失败任务」等产品页（若未来扩展 WebChat）。 |
| **长任务恢复「产品级」指标** | 有检查点字段与恢复入口；**无**大规模重放测试与 SLA 类验收。 |
| **V5 平台化** | 多 workspace 治理、插件市场与签名校验、组织级策略中心、运营大盘与告警等 —— 见 [`archive/specs/prd-v5.md`](./archive/specs/prd-v5.md)，状态为 **规划中**。 |

---

## 3. 可选增强（不阻塞主线）

- Trace：路径解析统一到单模块；人类可读摘要；HTTP 查询 API；日志轮转策略。
- MCP：Streamable HTTP / SSE；`RoutingMcpSdkClient` 更重集成测。
- 运维：部署/备份/日志权限的完整手册；拒绝原因字段与前端命名进一步统一。

---

## 4. 版本节奏（备忘）

- **V3**：工程化底座（测试、工具、trace、安全）— 主线已落地。  
- **V4**：协作与任务流 — **核心已落地**，上表「未完成」为加深项。  
- **V5**：平台与生态 — **未启动**，见归档 PRD。

详细历史需求与原文见 **[`archive/README.md`](./archive/README.md)**。
