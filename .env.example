# OneClaw 环境变量示例（复制为 .env 后按需修改；勿将含密钥的 .env 提交版本库）

# --- Ollama ---
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL_NAME=qwen2.5:3b
OLLAMA_STREAMING=false
OLLAMA_TEMPERATURE=0.3
OLLAMA_TOP_P=0.9
OLLAMA_TOP_K=40
OLLAMA_REPEAT_PENALTY=1.2
OLLAMA_NUM_PREDICT=512

# --- Gateway ---
PORT=3000
BIND_HOST=127.0.0.1
WEBCHAT_TOKEN=

# 界面语言：zh（中文）或 en（英文）；影响 TUI、Web 默认语言、对话摘要提示语等
# ONECLAW_UI_LOCALE=zh

# --- 数据与目录（默认 ~/.oneclaw 亦可）---
# ONECLAW_DATA_DIR=
# ONECLAW_USER_WORKSPACE_DIR=
# ONECLAW_SKILLS_DIR=

# 文件工具（read_file / search_files / apply_patch）额外允许根，逗号或分号分隔（仍包含主 workspace）
# 例（Windows）: ONECLAW_FILE_ACCESS_EXTRA_ROOTS=D:\Projects,E:\Docs
# ONECLAW_FILE_ACCESS_EXTRA_ROOTS=
# 落在以下绝对路径前缀下的路径一律禁止读写（即使在上面的允许根内）
# ONECLAW_FILE_ACCESS_DENIED_PREFIXES=C:\Windows,C:\Program Files

# 未命中 pathRules 时的默认级别：read | write | full（细粒度见 file-access.json）
# ONECLAW_FILE_ACCESS_DEFAULT=full

# 可选：合并 JSON（默认若存在则读取 {ONECLAW_DATA_DIR}/config/file-access.json）；网关会监听该文件并热重载
# ONECLAW_FILE_ACCESS_CONFIG=

ONECLAW_CHAT_CONTEXT_MAX_MESSAGES=30
ONECLAW_CHAT_SUMMARIZE_THRESHOLD=20

# --- 工具与安全 ---
# 每轮注入「移动文件先写目标再删源 / 优先单条 move」的 system 提示，默认 true（可减少误删）
# ONECLAW_FILE_RELOCATION_SAFETY_HINT=true
ONECLAW_EXEC_ENABLED=true
# ONECLAW_EXEC_TIMEOUT_MS=30000
# ONECLAW_EXEC_DENIED_PATTERNS=
# Windows：cmd 输出多为 GBK；默认按 cp936 解码 stdout/stderr。若终端已 chcp 65001 且输出 UTF-8，可设为 utf8
# ONECLAW_EXEC_STDOUT_ENCODING=utf8

# 内置 fetch_url / http_request：出站 HTTP（QQ 渠道策略上禁用；内网地址默认拒绝）
# ONECLAW_FETCH_URL_ENABLED=true
# ONECLAW_FETCH_TIMEOUT_MS=30000
# ONECLAW_FETCH_MAX_RESPONSE_CHARS=400000
# ONECLAW_FETCH_ALLOW_PRIVATE_HOSTS=false

# --- V4 任务：带 taskId 对话时 exec/apply_patch 是否需先人工批准（默认 true）---
# ONECLAW_TASK_HIGH_RISK_APPROVAL=true

# 无 task 聊天：点「批准」或回复「同意」后，为当前挂起工具增加的可执行次数（默认 1 = 每次高危调用都要再确认）
# ONECLAW_CHAT_RISK_SESSION_APPROVAL_BURST=1
# 一次批准是否对所有内置高危工具同时放行（默认 false；true 时易一次同意多次敏感操作）
# ONECLAW_CHAT_RISK_SESSION_APPROVE_ALL_TOOLS=false

# TUI/Web 智谱对话要收字流时建议开启（Ollama 带工具时服务端仍可能非流式）
# ZHIPU_STREAMING=true
# 带 tools 时是否走智谱 SSE 真流式（默认 false，用非流式再本地推一次 delta，避免空包/挂起；需真逐字再设 true）
# ZHIPU_STREAM_TOOLS=true
# 智谱 API「思考」链；若打开后界面仍出现 think 标签，保持 false 并依赖服务端 strip（默认 false）
# ZHIPU_THINKING_ENABLE=false

# --- 可选：QQ 渠道 ---
# ONECLAW_QQ_BOT_ENABLED=false
# ONECLAW_QQ_BOT_API_BASE_URL=
# ONECLAW_QQ_BOT_TOKEN=

# --- 可选：定时日报 ---
# ONECLAW_DAILY_REPORT_SCHEDULE_ENABLED=false
# ONECLAW_DAILY_REPORT_SCHEDULE_HOUR=18
# ONECLAW_DAILY_REPORT_SCHEDULE_MINUTE=0

# --- 可选：MCP（优先级：ONECLAW_MCP_SERVERS_FILE 存在则读该文件 > ONECLAW_MCP_SERVERS 内联 JSON > {ONECLAW_DATA_DIR}/config/mcp-servers.json）---
# ONECLAW_MCP_SERVERS=
# ONECLAW_MCP_SERVERS_FILE=
# 仓库内示例：workspace/config/mcp-servers.*.example.json（复制到上述 JSON 后按需填密钥）
# 常见 MCP 环境变量（写入 mcp-servers.json 的 env 字段，勿提交真实密钥）：
# BRAVE_API_KEY — Brave Search API（公网搜索）
# GITHUB_PERSONAL_ACCESS_TOKEN — GitHub MCP

# 内置 web_search 工具（Brave Web Search API，与 MCP 独立）：任选其一
# ONECLAW_BRAVE_API_KEY=
# BRAVE_API_KEY=

# 任务模板扩展 JSON 路径（默认 {ONECLAW_DATA_DIR}/config/task-templates.json），亦可在 Web「工作区」编辑
